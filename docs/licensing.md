# Licensing

Nothing enters this repository unless its licence permits redistribution from a
public repository and use in a publicly served application. Asset records carry
`licence` (SPDX, `CC0-1.0`, or `own-work`) and `source` (URL or `own-work`);
`licence-manifest.json` lists every tracked binary with sha256.
