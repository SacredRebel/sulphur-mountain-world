# C0 done — read the pack

**Date:** 2026-09-19  
**Commit message:** `eco(C0): survey the pack and the Oak Leaf walk contract`

## Delivered

- `docs/plans/C0-survey.md` — full written survey:
  - every pack file and how `pack.json` indexes them
  - `vision.geojson` schema and all 18 zone ids (all Points)
  - `oak-leaf.py` / `glb.py` `extras.walk` contract, reusable pieces, z-negation
  - pack frame vs model frame
  - `materials.json` inventory + building surfaces still missing (C1b)
  - proposed footprint and storey count for each of the 17 zones without geometry
  - LiteReality-Agent noted as the interior route (Notion C7)

## Not done (by design)

- No models, no toolkit, no materials extension, no `models.json`.

## Notion vs chat brief

Control surface is Notion. C7 on the board is interiors (LiteReality), not stations/events. Chat-brief C7/C8 items are not board phases yet.
